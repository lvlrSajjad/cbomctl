---
name: Policy rule — new, change, or verification
about: Add, correct, or verify a rule in a jurisdiction pack
labels: policy-pack
---

## Rule

Pack: `<bsi-de | anssi-fr | asd-au | cnsa-2.0 | us-eo14412 | eu-roadmap>`
Rule id: `<id, or "new">`

## Primary source

- URL (allowlisted host only):
- Document title and edition:
- Section / page read:
- Date read:

> A vendor blog, consultancy summary, or news article is not a primary source.
> If you have only secondary sources, say so — the rule stays
> `needs_verification` and that is a fine outcome.

## What the source actually says

Quote it. The exact verb matters: *recommends*, *requires*, *must*, *should*.

## Classification

- `binding`:
- `hybrid` (and the `applies_to.purpose` it is scoped to):
- `rationale`:
- `deadline` / `deadline_state`:

## Ambiguity

Anything the primary text leaves unclear. This belongs in
`docs/policy-sources.md` rather than being resolved by judgement.
